# The Unlived Life

An interactive visual essay based on a Bhandara message by Daaji.

This is a static site with no build process — `index.html` is a single self-contained HTML file with inline CSS and vanilla JS.
